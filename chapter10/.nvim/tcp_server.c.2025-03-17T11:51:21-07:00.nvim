#include <stdio.h>
#include <sys/socket.h>

int main() {

  printf("Server!\n");
  // 1. SOCKET - TCP Server on my local computer
  int socket(AF_UNIX, int type, int protocol);
  // 2. BIND
  // 3. LISTEN
  // 4. ACCEPT
  // 5. READ
  // 6. WRITE
}
