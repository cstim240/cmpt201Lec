#include <stdio.h>
#include <unistd.h>

int main() {
  // print the pid of a parent and pid of a child using getpid and getppid
  pid_t pid = fork();

  if (pid == 0){
    //if pid is 0, this is the child process
    printf("PARENT process is: %s, CHILD process is: 
  }
}
