#include <stdio.h>
#include <unistd.h>

int main() {
  // print the pid of a parent and pid of a child using getpid and getppid

  pid_t processID = fork();

  if (processID == -1) {
    printf("Forking forked up!\n");
  } else if (processID > 0) {
    pid_t processID = getpid();
    
  }
}
